# Stack per AGENTS.md. Shared across all three lanes -- G5 owns this file.
fastapi
uvicorn[standard]
pydantic-settings
requests

# lane I -- decode, detect, track, OCR
av
# Pinned to the 4.10 line: OpenCV 5.x changes cv2.minAreaRect / findContours return
# conventions, which silently shifts deskew() and the plate proposal. Same reasoning as
# the ultralytics pin (#45) - a CV change between now and the demo invalidates I7's numbers.
# ultralytics pulls plain opencv-python too; keep both on the same 4.10 line.
opencv-python-headless>=4.10,<4.11
numpy<2.4
# Pinned to 8.3: 8.4 dropped BYTETracker's `frame_rate` arg and changed how `track_buffer`
# maps to max_frames_lost (#45). tracker.py tolerates both, but the I7 accuracy numbers were
# measured on 8.3 and a silent detector/tracker change before the demo would invalidate them.
ultralytics>=8.3.0,<8.4
lap
paddleocr
paddlepaddle
# fourth OCR reader: a CCT trained end to end on plates (not scene text), ONNX runtime.
# Fails on a different axis from the scene-text readers, which is what the vote wants.
fast-plate-ocr
onnxruntime
# trained licence-plate *detector* (YOLOv9-t, ONNX) for the plate-localisation step -- finds
# the small / skewed / low-contrast plates the classical blackhat proposal misses. Optional:
# plate.py falls back to propose() when it is not installed.
open-image-models

# lane D -- persistence, matching, RBAC, export
psycopg2-binary
redis
minio
pyjwt
argon2-cffi
python-multipart
weasyprint

# lane G -- gateway drivers
onvif-zeep
# numpy is not optional here: PyAV's frame.to_ndarray() imports it lazily, so
# without it the drivers decode fine and then fail per frame.
numpy

# shared
python-ulid
pytest

# lane D -- added when lane D merged: object storage client, PDF, the API test client
# and the socket client the WebSocket tests drive a real server with.
boto3
httpx2
pytest-timeout
python-dotenv
reportlab
websockets

# Test-only, found missing when lane I merged: tests/test_i_publish.py imports fakeredis
# and services/worker publishes metrics through prometheus_client. Without these the lane I
# tests do not collect on a clean machine or in CI.
fakeredis
prometheus-client
