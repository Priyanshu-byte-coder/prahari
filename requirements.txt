# Stack per AGENTS.md. Shared across all three lanes -- G5 owns this file.
# Merged for QA_testing: union of lane D, lane G and lane I requirements.
fastapi
uvicorn[standard]
pydantic-settings
requests
python-dotenv

# lane I -- decode, detect, track, OCR
av
opencv-python-headless
numpy
ultralytics
lap
paddleocr
paddlepaddle

# lane D -- persistence, matching, RBAC, export
psycopg2-binary
redis
minio
pyjwt
argon2-cffi
python-multipart
weasyprint
boto3
reportlab
httpx2

# lane G -- gateway drivers
onvif-zeep
# numpy is not optional here: PyAV's frame.to_ndarray() imports it lazily, so
# without it the drivers decode fine and then fail per frame.

# shared
python-ulid
websockets
pytest
pytest-timeout
